# 데이즈온 대시보드 — 상품 분석 기능 설계도 (v6, 확정)

v5 이후 실제 원장 데이터(`매출대시보드_원장.xlsx`)를 직접 열어 검토하며 발견된 핵심 이슈 반영:
① 판매수량이 "박스"가 아니라 "세트/건수" 단위였던 문제, ② 콤보(복합) 상품의 구성 분리,
③ 분석 단위를 SKU에서 "매체×중분류×복합상품여부"로 최종 확정.

---

## §0. 사전 데이터 정제 (신규 — 최우선 작업, 이미 1차 완료)

### 문제 발견
원장의 `실 판매`(qty) 필드는 "세트/건수" 단위였다. 예: `파라홈3`(3구성 상품) 실판매 13건 = 실제 박스수 39개.
`qty`를 그대로 "판매수량"으로 쓰면 config가 다른 상품 간 물량 비교, 개당단가, 브리지의 물량효과 전부 틀어짐.

### 원장에 추가한 필드
| 컬럼 | 내용 |
|---|---|
| O열 `단위 박스` | 상품명에서 파싱한 1차 구성수 (예: `파라홈3` → 3) |
| P열 `단위 박스(2)` | 콤보 상품의 2차 구성수 (예: `맥비18 맥샴푸2` → O=18, P=2) |

### 파싱 규칙
1. 상품명에서 숫자를 추출하되, **무게/용량 단위(g/ml/kg)가 바로 붙은 숫자는 제외** (예: `블병[30g]10` → "30"은 무시, "10"만 채택)
2. 숫자가 아예 없는 상품명(예: `올포`, `파라`, `그린아세로라` — 대분류는 있으나 config 표기 없음) → 단위박스 = 1 (낱개 판매로 간주)
3. 숫자가 1개 → O에 그 값, P는 비움
4. 숫자가 2개(콤보) → O=첫 번째, P=두 번째. **단, 두 번째 구성이 "체험분" 또는 "[체]"(체험 약어)면 증정품으로 보고 버림 — P는 비우고 O만 유지** (예: `올포12 체험분1` → O=12만, P 비움)
5. 숫자가 3개 이상인 특수 상품명(스펙 설명이 섞인 경우 등)은 자동판정하지 않고 O/P를 비운 채 수동검토 목록에 별도 표기

### 파생 필드 (분석 단계에서 계산)
```
총박스수 = O + (P가 있으면 P, 없으면 0)
복합상품여부 = P열에 값이 있으면 TRUE, 없으면 FALSE
```

### 콤보 상품의 카테고리 귀속 원칙
`중분류` 필드가 이미 콤보를 반영해 별도 값으로 기록된 경우(예: `맥주효모&샴푸`)는 그대로 독립 카테고리로 취급 — 매출을 컴포넌트별로 쪼갤 근거가 없으므로 O+P 합산값을 그 중분류 전체에 귀속.

**단, 원장 점검 결과 중분류 라벨링이 일관되지 않음을 확인**: 일부 중분류(`샴푸490ml`, `크랜베리`, `트리플`, `아세로라`, `샴푸400ml`, `파라다이스` 등 6개)는 콤보 상품이 섞여 있는데도 라벨이 단일상품인 것처럼 표기되어 있음. → **중분류 텍스트의 "&" 표기에 의존하지 않고, `복합상품여부`(P열 존재 여부)를 콤보 판별의 유일한 기준으로 사용**. 이렇게 하면 라벨링 일관성 문제를 데이터로 우회할 수 있음.

---

## §1. 진단 단위 — "SKU" 대신 "매체 × 중분류 × 복합상품여부"

### 확정 이유
- SKU(상품명) 표준화는 사은품·구성 표기 차이로 지저분할 것으로 예상했으나, 실제로는 `media`/`sub`(중분류)가 원장에서 이미 깨끗한 필드였고, 단위박스 파싱까지 끝내고 나니 상품명 원문도 충분히 분석 가능한 수준임을 확인.
- 새 SKU 마스터 코드 체계를 별도로 구축하지 않고, **상품명 원문을 그대로 집계키(하위 드릴다운 키)로 사용**.

### 집계 구조 (2단)
```
[요약 레벨]  매체 × 중분류 × 복합상품여부
   └─ [드릴다운 레벨]  상품명(원문) 단위
```

### 요약 테이블 컬럼 (예: "방송 × 맥주효모 × 단품")
| 컬럼 | 계산식 |
|---|---|
| 매체/중분류/복합여부 | 그룹 키 |
| 기준월 매출 | `Σ sales` |
| 총박스수 | `Σ (O + P)` |
| 박스당 단가 | `매출 ÷ 총박스수` |
| 기준월의 전월 대비 | `(당기-전기)/전기` |
| 최근 3개월 추세 | 스파크라인 |
| 온라인 MER(매체 귀속 아님) | §5 참고 |
| 생애주기 | §3 |
| 진단 신호 | §6 |

화면 상단 표시: `분석 기준월: 2026년 7월(마감)` (v5와 동일, 진행월과 분리)

### 드릴다운 테이블 컬럼 (상품명 단위) — v5의 §4(구성별 경쟁력 비교) 흡수
| 컬럼 | 계산식 |
|---|---|
| 상품명 | 원문 |
| 단위박스(O) / 단위박스(2)(P) | 원장값 그대로 표시 |
| 총박스수 | O + P |
| 매출 | `Σ sales` |
| 박스당 단가 | `매출 ÷ 총박스수` |
| 상대 단가 지수 | `해당상품 박스당단가 ÷ 동일(매체×중분류×복합여부) 그룹 내 최고 박스당단가` (v5 §4 "할인깊이"를 대체한 지표, 방향 고정됨) |
| 판매 총박스수 비중 | 그룹 내 비중(%) — 카니발라이제이션 탐색용 |

카니발라이제이션 자동판정(상관계수 기반)은 v5와 동일하게 §8 마지막 단계로 유예. 1차는 그룹 내 상품명별 월별 비중 변화 그래프만 제공.

---

## §2. 매출 증감 브리지 — 단위를 "(매체,중분류,복합여부)"로 치환

v5의 브리지 공식 구조(공통그룹/신규/재활성/소멸 분리, 시장물량효과/상품믹스효과 분해, 허용오차 검증)는 그대로 유지하되, "SKU" 자리를 전부 **"(매체,중분류,복합상품여부) 조합"**으로, "수량(Q)"은 전부 **"총박스수"**로 치환.

```
공통그룹 수량효과 = Σ (Box1 - Box0) × P0     (Box = 총박스수, 공통 그룹만)
가격효과         = Σ Box1 × (P1 - P0)         (P = 매출÷총박스수)
```
나머지 신규/재활성/소멸 그룹 분리, 부호 정의(양수 저장), 검증식(허용오차 반올림 후 1원), 미마감월 제외 규칙은 v5와 완전히 동일 — 단위만 바뀜.

---

## §3. 생애주기 분류 — 단위를 "(매체,중분류,복합여부)"로 치환

v5의 판정 순서(원장누락의심→휴면→신규→비교기간부족→하락→성장→유지), 성장률 분모 0 예외처리, "하락" 정의, 유의미매출 기준, 재활성 최종분류 규칙(신규 vs 판정유보+진단신호) 전부 동일 — 대상만 "(매체,중분류,복합여부)" 그룹 단위로 적용.

---

## §4. (v5의 §4를 §1 드릴다운으로 흡수 — 별도 섹션 없음)

---

## §5. MER 명칭

v5와 동일. 기본 노출: **"온라인 MER(매체 귀속 아님)"** = 온라인 채널 매출 ÷ 해당 그룹 온라인광고비.

---

## §6. 진단 신호

v5의 8개 신호와 수치 기준표 동일, 적용 단위만 "(매체,중분류,복합여부)" 그룹으로 치환. 계산 전 공통 전제조건(수량 0 제외, 전월매출 0 처리, 진행월 제외)도 동일.

---

## §7. B단계 원가배분

v5와 동일. `추정 매출총이익`(카테고리 평균원가율) / `추정 기여이익`(+매출비례 비용배분) 명칭 유지. `PURCHASE` 원가 데이터도 매체×중분류 단위로 배분 가능한지 §8에서 재확인.

---

## §8. 구현 순서 (v6)

1. **[완료]** 단위박스(O/P) 1차 파싱 및 콤보/체험분 처리 — 원장 반영 완료, 3건(스펙설명형 2건 + 검증필요분)만 수동검토 대기
2. `복합상품여부` 플래그 및 `총박스수` 파생 필드를 `build.py`에 반영 (구글시트 원장 → JSON 집계 단계에 추가)
3. 매체×중분류×복합상품여부 요약 진단 테이블 (원가 컬럼 숨김)
4. 상품명 드릴다운 테이블 (박스당단가, 상대단가지수)
5. 생애주기·진단 신호 통합 (그룹 단위 적용)
6. 매출 증감 브리지 (그룹 단위, 총박스수 기준)
7. 원가 마스터 구축 (③)
8. 그룹별 공헌이익 적용
9. 데이터 기간 충분해진 후 카니발라이제이션 후보 분석

### 남은 확인 사항
- 수동검토 2건(`파라다이스그레인버닝...`, `파인애플맛...`)의 정확한 단위박스 값
- `build.py`가 구글시트에서 O/P열(단위박스)을 읽어오도록 파싱 로직 추가 필요 — 현재 `build.py`는 이 두 컬럼을 참조하지 않음
