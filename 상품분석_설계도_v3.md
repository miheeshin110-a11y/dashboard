# 데이즈온 대시보드 — 상품 분석 기능 설계도 (v3)

v2 검토 피드백 반영: 브리지 변수명 중복 제거, 검증 허용오차, 생애주기/데이터상태 분리, 신규·재활성 구분, 채널ASP 주의문구, 진단신호 수치기준, 순서 오탈자, SKU 집계키 정의.

---

## 1. SKU 진단 테이블

### SKU 집계키 (신규 — 명시)
```
집계키 = 표준 SKU 코드 (있으면)
        정규화된 prod + config + sub (없으면)
```
동일 상품명이 다른 구성/기획세트를 가리키는 경우를 막기 위해 `prod` 단독이 아니라 `config`, `sub`까지 포함한 복합키를 기본으로 사용. (표준 SKU 코드 마스터는 §7에서 구축)

### 기본 화면 컬럼 (11개)
| 컬럼 | 계산식 |
|---|---|
| SKU | 집계키 기준 표시명 |
| 카테고리 | `cat` |
| 당월 매출 | `Σ sales (당월)` |
| 판매수량 | `Σ qty (당월)` |
| ASP | `매출 ÷ 판매수량` |
| 개당단가 | `ASP ÷ config` |
| 전월 대비 | `(당월-전월)/전월` |
| 최근 3개월 추세 | 스파크라인 |
| 온라인 MER (매체 귀속 아님) | `온라인 채널 매출 ÷ SKU 온라인광고비` |
| 생애주기 | §3 (우선순위 규칙, 데이터상태 분리) |
| 진단 신호 | §6 (수치 기준 포함) |

### 상세 행 (클릭 시 펼침)
- 방송광고비·PPL 카테고리 배분 추정치 ("배분 추정" 라벨 필수)
- 판매 지속월수, 연속 무매출 개월수
- 매입원가/공헌이익: **B단계 전 컬럼 자체 숨김**

### 선행 작업: SKU 표준화
`ONLINE.prod` ↔ `RAW.prod` 매핑률 사전 점검 필수. 표준화 안 된 상태로 시작하면 이후 전 지표가 부정확해짐.

---

## 2. 매출 증감 브리지

### 변수명 분리 (v2 이중계산 버그 수정)

**1단계 — 3그룹 분리**
1. 공통 SKU (전월·당월 모두 판매)
2. **신규** SKU (원장 전체 기준 최초 판매가 당월)
3. **재활성** SKU (과거 판매 이력 있으나 전월엔 무매출, 당월에 재등장)
4. **소멸** SKU (전월엔 판매, 당월엔 무판매)

(신규/재활성을 분리하는 이유: 상품 분석 관점에서 의미가 다름 — 신규는 "새 상품 기여", 재활성은 "죽었던 상품 부활". 화면이 복잡하면 합쳐 표시하되 툴팁에 구성 금액 분리 표기.)

**2단계 — 공통 SKU 수량효과·가격효과 (변수명 확정)**
```
공통SKU 수량효과 = Σ (Q1 - Q0) × P0
가격효과         = Σ Q1 × (P1 - P0)

→ 공통 SKU 매출 증감 = 공통SKU 수량효과 + 가격효과   (항상 정확히 성립)
```

**3단계 — (선택) 상품믹스 세부 분해**
```
시장 물량효과 = (전체 판매수량_당월 − 전체 판매수량_전월) × 전월 평균ASP
상품믹스 효과 = 공통SKU 수량효과 − 시장 물량효과
```
※ `상품믹스 효과`는 `공통SKU 수량효과`의 **재분해 결과**이지 별도 항목이 아님. 두 방식을 섞어 쓰지 않는다.

### 검증식 (둘 중 하나만 사용, 섞지 않음)

**단순 버전:**
```
공통SKU 수량효과 + 가격효과 + 신규효과 + 재활성효과 − 소멸효과 = 전체 매출 증감
```

**세부 분해 버전:**
```
시장물량효과 + 상품믹스효과 + 가격효과 + 신규효과 + 재활성효과 − 소멸효과 = 전체 매출 증감
```

### 허용오차 (수정)
JS 부동소수점 오차 감안, 엄격한 1원 기준 대신:
```
표시 금액을 원 단위로 반올림 → 잔차 = 실제 매출 증감 − 브리지 효과 합계(반올림 후)
검증 통과 = |잔차| ≤ 1원
```
반올림 전 raw 값으로 비교하지 않는다.

### 채널 믹스 (표현 추가 완화)
채널 ASP가 낮아진 원인은 두 가지가 섞여 있을 수 있음:
```
채널 ASP 변화 = 동일 SKU의 채널 판매가격 변화 + 채널 내 상품구성 변화
```
현재 `media` 단위 단순 집계로는 이 둘을 구분 못 함. 1차 화면 표현은 다음까지만:
- "평균 판매단가가 낮게 관측된 채널의 비중이 증가했다"
- "채널별 매출·수량 구성 변화"

("판매단가가 낮은 채널" 같은 채널 자체 속성으로 단정하는 표현 금지. SKU×media 단위 정밀 비교는 2차 과제.)

### 구현 순서
§8 기준 **5번째** (v2에서 "6번째"로 잘못 표기된 부분 정정).

---

## 3. 상품 생애주기 분류

### 구조 분리 (신규 — 생애주기 vs 데이터상태)

**생애주기** (하나만 선택):
`휴면 → 신규 → 하락 → 성장 → 유지 → 판정불가`

**데이터상태** (별도 태그, 생애주기와 동시 표시 가능):
`정상 / 관측기간 부족 / 음수매출 / 원장누락 의심`

### 판정 순서 (수정 — 모든 이상 조건을 하나로 묶지 않음)
1. 원장누락 의심 → **판정유보**
2. 과거 유의미 매출 이력 + 최근 2개월 연속 무매출 → **휴면**
3. 최초 판매월로부터 3개월 이내 → **신규**
4. 비교 가능 기간(6개월) 부족 → **판정유보**
5. 하락 (§ 하락 정의 참고)
6. 성장 (최근 3개월 성장률 ≥ +10%)
7. 유지 (그 외)

### "하락" 정의
```
G_t   = (t-2~t월 평균) / (t-5~t-3월 평균) - 1
G_t-1 = (t-3~t-1월 평균) / (t-6~t-4월 평균) - 1
하락 = G_t ≤ -10%  이고  G_t-1 ≤ -10%
```
최소 7개월 데이터 필요. 부족 시 폴백:
```
하락(폴백) = 최근 3개월 평균이 이전 3개월 평균보다 10% 이상 감소
```

### 유의
1회성 대형 방송 매출로 성장률이 왜곡될 수 있음 — 2차 개선 과제(해당 월 방송 매출 비중 이상치면 별도 표기).

---

## 4. 구성별(용량) 경쟁력 비교

### 상대 단가 지수
```
상대 단가 지수 = 해당구성 개당단가 ÷ 동일 중분류 최고 개당단가
```
"할인깊이" 명칭 사용 안 함.

### 유의사항
- 사은품/체험분 포함 행은 집계에서 제외 옵션 제공
- 카니발라이제이션 자동판정은 §8 마지막 단계로 이동. 1차는 **구성별 월별 판매 비중 변화 그래프**만 제공

### 카니발라이제이션 재도입 조건 (향후, §8-8단계)
"카니발라이제이션 의심 후보"로만 명명. 최소 조건: 공통 판매기간 6개월↑, 양쪽 유의미 판매량, 상관계수 ≤ -0.5, 비중 상승/하락 동시 발생, 품절·신규출시월 제외.

---

## 5. MER 명칭

| 표현 | 정의 | 화면 라벨 |
|---|---|---|
| SKU Blended MER | SKU 전체 매출 ÷ 온라인광고비 | 참고용, 기본 노출 안 함 |
| **온라인 MER(매체 귀속 아님)** | 온라인 채널 매출 ÷ SKU 온라인광고비 | **기본 노출** |
| 전체 Blended MER | 전체 매출 ÷ 전체 확인 광고비 | 카테고리 레벨에서만 |
| ROAS | 광고 귀속 매출 기반 | 현재 데이터로 산출 불가 |

---

## 6. 진단 신호 (수치 기준 명시 — 신규)

기준값은 코드 상 **설정 상수로 분리**해 추후 조정 가능하게 구현.

| 신호 | 초기 조건 |
|---|---|
| 매출↑ 단가↓ | 매출 전월대비 ≥ +10%, ASP 전월대비 ≤ -5% |
| 수량↓ 가격↑ | 판매수량 전월대비 ≤ -10%, ASP 전월대비 ≥ +5% |
| 2개월 무매출 | 과거 유의미 매출 존재 + 최근 2개월 매출 0 |
| 광고비↑ 매출↓ | 광고비 전월대비 ≥ +10%, 해당 채널 매출 전월대비 ≤ -10% |
| 대형구성 비중 급증 | 최대구성 판매수량 비중 전월대비 ≥ +15%p |
| 단일 채널 의존 | 최대 채널 매출 비중 ≥ 70% |
| 신규 성장 | 최초 판매 3개월 이내 + 월매출 연속 증가 |
| 판정 데이터 부족 | 비교 가능 판매기간 6개월 미만 |

---

## 7. B단계 원가배분

### 명칭
| 산출 방식 | 표시 명칭 |
|---|---|
| 카테고리 평균원가율 적용 | **추정 매출총이익** |
| + 방송광고비·PPL 매출비례 배분 | **추정 기여이익** (매출 비례 비용배분 기준) |

"공헌이익"은 ③ SKU-원가 매핑 테이블 완료 후에만 사용.

### SKU-원가 매핑 마스터 (③, 최종 목표)
- **표준 SKU 코드** (§1 집계키의 근거가 됨)
- 상품명 / 구성 수 / 단품 내용량
- 제품원가 / 포장비 / 출고·물류비 / 채널 수수료율
- 적용 시작일·종료일 (원가는 시점별 변동 — 필수)

---

## 8. 구현 순서

1. SKU 표준화 및 데이터 품질 점검 (매핑률 검증, 표준 SKU 코드 체계 여부 확인)
2. SKU 진단 테이블 기본형 (원가 컬럼 숨김, 11개 핵심 컬럼)
3. 생애주기·데이터상태 분리 + 판정 순서 적용 + 진단 신호(수치기준 상수화) 통합
4. 구성별 상대 단가와 판매 비중 비교
5. 매출 증감 브리지 (변수명 분리, 허용오차 기반 검증 포함)
6. 원가 마스터 구축 (③)
7. SKU별 공헌이익 적용 (추정치 → 정식 공헌이익 전환)
8. 데이터 기간 충분해진 후 카니발라이제이션 후보 분석
