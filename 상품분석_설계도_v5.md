# 데이즈온 대시보드 — 상품 분석 기능 설계도 (v5, 확정)

v4 검토 피드백 반영: 당월→기준월 명칭 통일, 오탈자 수정, 절 번호 수정, 재활성 후보 최종분류 명확화. 이번 라운드로 설계 확정 — 최종 판정: 분석논리/계산정합성/데이터제약표시/구현순서/운영상오판방지 전부 적합, 개발 착수 조건은 SKU 매핑 품질 기준 통과.

---

## 1. SKU 진단 테이블

### SKU 집계키
```
집계키 = 표준 SKU 코드 (있으면) / 없으면 정규화된 prod + config + sub
```

### 화면 상단 표시 (신규)
```
분석 기준월: 2026년 7월(마감)
```
생애주기·브리지는 항상 최근 마감월을 사용하므로, "당월"이라는 표현은 진행월 KPI와 혼동을 줄 수 있어 **"기준월"로 통일**한다. (진행월 실시간 KPI 화면과 명확히 분리)

### 기본 화면 컬럼 (11개)
SKU / 카테고리 / 기준월 매출 / 판매수량 / ASP / 개당단가 / 기준월의 전월 대비 / 최근 3개월 추세 /
온라인 MER(매체 귀속 아님) / 생애주기 / 진단 신호

상세 행: 방송광고비·PPL 배분 추정치("배분 추정" 라벨), 판매 지속월수, 연속 무매출 개월수.
원가/공헌이익 컬럼은 B단계 전 숨김.

### 선행 작업: SKU 매핑 품질 기준 (구체화)
매핑률 점검을 "느낌"으로 끝내지 않고 아래 3개 지표로 통과 기준을 둔다:
- **매출금액 매핑률 ≥ 99%** (가장 중요)
- 온라인광고비 매핑률 ≥ 95%
- 건수 기준 매핑률 (참고용)
- 미매핑 상위 항목은 수동 검토 완료 후 진행

---

## 2. 매출 증감 브리지

### 그룹 분리
1. 공통 SKU (전월·당월 모두 판매)
2. 신규 SKU (원장 전체 기준 최초 판매가 당월)
3. 재활성 SKU (과거 이력 있으나 전월 무매출, 당월 재등장)
4. 소멸 SKU (전월엔 판매, 당월엔 무판매)

### 공통 SKU 수량·가격효과
```
공통SKU 수량효과 = Σ (Q1 - Q0) × P0     (공통 SKU만)
가격효과         = Σ Q1 × (P1 - P0)     (공통 SKU만)
→ 공통 SKU 매출 증감 = 공통SKU 수량효과 + 가격효과
```

### 상품믹스 세부 분해 — **공통 SKU 수량만 사용 (v3 최종 수정)**
```
Q0_common = 공통 SKU 전월 수량 합계
Q1_common = 공통 SKU 당월 수량 합계
전월 공통SKU 평균ASP = 공통 SKU 전월 매출 합계 ÷ Q0_common

시장 물량효과 = (Q1_common - Q0_common) × 전월 공통SKU 평균ASP
상품믹스 효과 = 공통SKU 수량효과 - 시장 물량효과
```
※ "전체 판매수량"(신규·재활성·소멸 포함)을 쓰면 안 됨 — **공통 SKU 집합으로 한정**해야 아래가 정확히 성립:
```
시장 물량효과 + 상품믹스 효과 = 공통SKU 수량효과
```

### 신규/재활성/소멸 부호 정의 (명시)
```
신규효과   = 당월 신규 SKU 매출 합계     (양수로 저장)
재활성효과 = 당월 재활성 SKU 매출 합계   (양수로 저장)
소멸효과   = 전월 소멸 SKU 매출 합계     (양수로 저장)
```
검증식에서는 소멸효과를 **빼고**, 워터폴 차트에는 `-소멸효과`로 전달. (음수로 저장한 뒤 다시 빼는 이중부호 처리 금지 — 항상 양수로 저장 → 사용처에서 부호 결정)

### 검증식 (둘 중 하나만 사용)
```
[단순]   공통SKU 수량효과 + 가격효과 + 신규효과 + 재활성효과 - 소멸효과 = 전체 매출 증감
[세부]   시장물량효과 + 상품믹스효과 + 가격효과 + 신규효과 + 재활성효과 - 소멸효과 = 전체 매출 증감
```

### 허용오차
표시금액 원단위 반올림 후 `|잔차| ≤ 1원`. 실패 시 화면에 표시 안 함.

### 미마감월 비교 방지 (신규 — 운영 기준)
진행 중인 월(예: 8월 20일 시점의 8월)을 완전한 전월과 비교하면 거의 모든 상품이 "하락"으로 잘못 판정됨.
```
생애주기 · 브리지 계산  → 최근 "마감월" 기준으로만 수행
실시간 KPI(전체 요약)   → 진행월 누적 표시는 별도로 허용, 진행월 배지 표시
```
진행월은 생애주기/브리지 판정 대상에서 제외.

### 채널 믹스
"채널 ASP 변화 = SKU 가격변화 + 채널내 구성변화" — 1차 화면은 "평균 판매단가가 낮게 관측된 채널 비중 증가" 수준까지만 표현 (v3와 동일, 변경 없음).

---

## 3. 상품 생애주기 분류

### 용어 통일 (v3 수정)
생애주기 6개 상태: **휴면 / 신규 / 하락 / 성장 / 유지 / 판정유보**
("판정불가"는 사용 안 함 — "판정유보"로 통일, 향후 데이터 추가 시 재판정 가능하다는 의미 전달)

데이터상태 (별도 태그): **정상 / 관측기간 부족 / 음수매출 / 원장누락 의심**

표시 예: `생애주기: 판정유보  ·  데이터상태: 관측기간 부족`

### 판정 순서
1. 원장누락 의심 → 판정유보 *(§ 원장누락 기준 참고 — 자동판정 무리하지 않음)*
2. 과거 유의미 매출 이력 + 최근 2개월 연속 무매출 → 휴면
3. 최초 판매월로부터 3개월 이내 → 신규
4. 비교 가능 기간(6개월) 부족 → 판정유보
5. 하락
6. 성장
7. 유지

### 성장률 분모 0 예외처리 (신규 — 필수 수정)
```
이전 3개월 평균 = 0, 최근 3개월 평균 = 0  → 휴면 또는 판정유보
이전 3개월 평균 < 0                        → 성장률 계산 제외, 데이터상태 = '음수매출'
```

**이전 3개월 평균 = 0, 최근 3개월 평균 > 0 인 경우 (재활성 후보) — 최종 분류 명확화:**
```
최초 판매 3개월 이내         → 생애주기 = '신규'
과거 판매 이력 존재 + 최근 재개 → 생애주기 = '판정유보'
                              → 진단 신호에 '재활성' 표시
                              → 3개월 데이터 누적 후 성장/유지/하락으로 재판정
```
(현재 6개 생애주기 상태를 유지하는 방식 — '재활성'을 정식 생애주기 상태로 추가하는 대안도 있으나, 관리 단순성을 위해 판정유보+진단신호 조합으로 처리)

Infinity% 등 비정상 성장률이 화면에 노출되지 않도록 반드시 사전 분기 처리.

### "하락" 정의
```
G_t = (t-2~t월 평균)/(t-5~t-3월 평균) - 1,  G_t-1 = (t-3~t-1월 평균)/(t-6~t-4월 평균) - 1
하락 = G_t ≤ -10% 이고 G_t-1 ≤ -10%   (최소 7개월 데이터 필요)
폴백(데이터 부족 시) = 최근 3개월 평균이 이전 3개월 평균보다 10% 이상 감소
```

### "유의미 매출" 기준 (신규 — 휴면 판정에 필요)
```
과거 유의미 매출 = 과거 어느 한 달 매출 ≥ 100만원  또는  과거 누적 판매수량 ≥ 10박스
```
절대 기준값은 설정 상수로 관리 (카테고리별 조정 가능하게).

### 원장누락 의심 기준 (신규 — 무리한 자동판정 지양)
1차 버전에서는 아래 조건이 있을 때만 태그. 애매하면 아예 태그 안 붙임(과잉 경고 방지):
- 카테고리 전체가 특정 월에 갑자기 0
- 기존 주요 채널 데이터가 특정 월에만 전부 사라짐
- 판매수량은 있으나 매출이 없음
- 매출은 있으나 SKU/카테고리 필드가 없음

---

## 4. 구성별(용량) 경쟁력 비교

변경 없음 (v3 동일): 상대 단가 지수 = 해당구성 개당단가 ÷ 동일 중분류 최고 개당단가.
카니발라이제이션 자동판정은 §8의 8단계로 유예, 1차는 구성별 월별 비중 변화 그래프만.

---

## 5. MER 명칭

변경 없음 (v3 동일): 기본 노출은 **"온라인 MER(매체 귀속 아님)"** = 온라인 채널 매출 ÷ SKU 온라인광고비.

---

## 6. 진단 신호

### 수치 기준 (v3 표 유지, 설정 상수로 분리)
매출↑단가↓ / 수량↓가격↑ / 2개월무매출 / 광고비↑매출↓ / 대형구성비중급증 / 단일채널의존 / 신규성장 / 판정데이터부족
— 각 신호별 임계값은 v3 표와 동일.

### 계산 전 공통 전제조건 (신규 — 필수)
```
전월 또는 당월 판매수량 ≤ 0     → 해당 SKU의 ASP 증감 기반 신호 계산 제외
전월 매출 = 0                    → "전월 대비 증감률" 대신 '신규/재활성' 자체를 신호로 표시
진행월(미마감)                   → 전월 대비 신호 계산 제외, 또는 동일 경과일수 기준 비교로 전환
```

---

## 7. B단계 원가배분

변경 없음 (v3 동일): ① = 추정 매출총이익, ①+방송광고·PPL 배분 = 추정 기여이익. "공헌이익"은 ③ SKU-원가 매핑 완료 후에만 사용. 마스터 컬럼: 표준SKU코드/상품명/구성수/단품내용량/제품원가/포장비/출고물류비/채널수수료율/적용시작일·종료일.

---

## 8. 구현 순서

1. **SKU 표준화 및 데이터 품질 리포트** — 매출금액 매핑률(≥99%), 온라인광고비 매핑률(≥95%) 검증 + 미매핑 상위 항목 수동 검토
2. SKU 진단 테이블 기본형 (원가 컬럼 숨김)
3. 생애주기·데이터상태 분리 + 분모 0 예외처리 + 진단 신호(전제조건 포함) 통합
4. 구성별 상대 단가와 판매 비중 비교
5. 매출 증감 브리지 (공통SKU 기준 상품믹스 분해, 부호 명시, 허용오차 검증, 미마감월 제외)
6. 원가 마스터 구축
7. SKU별 공헌이익 적용
8. 데이터 기간 충분해진 후 카니발라이제이션 후보 분석

---

## 다음 단계
설계는 이 시점에서 확장을 멈추고, **1번(SKU 표준화 및 데이터 품질 리포트)**을 실제로 돌려 원장의 실제 예외 케이스를 먼저 발견하는 것이 더 효율적입니다. 리포트 결과를 보고 2~8번 설계를 필요시 미세조정합니다.
